registerChapter({ chapter: "ch02-long-story-ch04-technical-02-backstage-00-the-backstage", content: function () { return "<h1 id=\"the-backstage\">The Backstage</h1>\n<p>The backstage is the hub for all the non-study and maintenance activities\nin a textbook.</p>\n<p>The main modules in the backstage are:</p>\n<ul>\n<li>The Avatar</li>\n<li>Notification system</li>\n<li>The Dashboard</li>\n</ul>\n<p>To make the system more flexible and the modules independent, the API is based on\nevents (i.e. Backbone events) and the event bus is the App object. This allows modules\nto work together even if one is missing.</p>\n<p>For example when an exercise is complete it can trigger a mood change event to\nthe avatar. If avatar module is not installed (for various reasons like licensing\nor policy) the message is ignored and nobody&#39;s hurt.</p>\n<p>The syntax for this is <code>App.trigger(&lt;message&gt;, &lt;param&gt;[, &lt;param&gt;,...])</code>\n(e.g. <code>App.trigger(&#39;avatar:say&#39;, &#39;Hello, world!! :)&#39;)</code>)</p>\n"; }});