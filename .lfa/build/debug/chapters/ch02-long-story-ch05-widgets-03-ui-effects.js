registerChapter({ chapter: "ch02-long-story-ch05-widgets-03-ui-effects", content: function () { return "<h1 id=\"parallax-effect\">Parallax Effect</h1>\n<p>While reading the lfa backends, I found that the <code>+parallax(path)</code> was already implemented in the <code>LFA</code> core, but for the sake of consistency, I transffered it here and wrote this page.</p>\n<div style=\"background-image: url('http://placekitten.com/g/1200/1200')\" class=\"parallax\"><h1>Hello Kitty!</h1></div><p>To use it you have to call <code>+parallax(path)</code> add the path.</p>\n<pre><code>+parallax(&#39;/path/to/local/or/online/image/&#39;)\n</code></pre>"; }});