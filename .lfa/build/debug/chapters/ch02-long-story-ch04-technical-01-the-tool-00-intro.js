registerChapter({ chapter: "ch02-long-story-ch04-technical-01-the-tool-00-intro", content: function () { return "<h1 id=\"lfa-tool\">LFA Tool</h1>\n<p><em>Learn Forward Author</em>, for short <em>LFA</em>,  is a tool (at this moment command line only) that is used to produce context-aware textbooks like this one. The way it works is simple: it takes some files that the editor writes (called the <em>source files</em>) and assembles them into a bigger and nicer output, the context-aware textbook. It is more than a transformation process.</p>\n<h2 id=\"installation\">Installation</h2>\n<p>Make sure you have <a href=\"http://nodejs.org/\">node.js</a> installed, then run:</p>\n<pre><code class=\"lang-bash\">$ npm install -g lfa\n</code></pre>\n<p>If you get permission issues you most likely have a dodgy node installation. You can probably circumvent it temporarily with <code>sudo -H npm install -g lfa</code>.</p>\n<h2 id=\"usage\">Usage</h2>\n<p>LFA is a command line tool. For the basic help, run:</p>\n<pre><code class=\"lang-bash\">$ lfa\n</code></pre>\n<h2 id=\"quick-start-guide\">Quick start guide</h2>\n<p>Let&#39;s create a new project:</p>\n<pre><code class=\"lang-bash\">$ lfa new my-first-book\n$ cd my-first-book\n</code></pre>\n<p>Your project structure should look something like this:</p>\n<pre><code class=\"lang-bash\">$ tree\n.\n|-- config.yml\n|-- css\n|   `-- master.styl\n|-- img\n|   `-- kitten.jpg\n`-- text\n    |-- ch01\n    |   |-- 00.jade\n    |   |-- 01.jade\n    |   `-- 02.jade\n    `-- ch02.jade\n</code></pre>\n<p><code>config.yml</code> is the main configuration file. This is where you set up project settings like the title or language of the book</p>\n<p>The <a href=\"http://jade-lang.com\">Jade</a> files in <code>text/</code> make up for the book&#39;s chapters. The table of contents is generated automatically based on the folder hierarchy.\nFor CSS, we use <a href=\"http://learnboost.github.io/stylus/\">Stylus</a>. You should include whatever styles you have in <code>css/master.styl</code> or in <code>@import</code>-ed stylus or CSS files.\nAny other assets can be placed in any other subdirectory of the project</p>\n<p>Let&#39;s start editing our new project. Before we do that, we start the watcher:</p>\n<pre><code class=\"lang-bash\">$ lfa watch\n</code></pre>\n<p>By now, a browser tab should have popped up for you. You can now start editing files with your <a href=\"http://en.wikipedia.org/wiki/Editor_war\">favorite text editor</a> and, as soon as you save, the watcher will reload the page for you.</p>\n<p>We built in some useful mixins and libraries (like <a href=\"http://getbootstrap.com/2.3.2/\">Bootstrap</a>, <a href=\"http://fortawesome.github.io/Font-Awesome/\">FontAwesome</a>, <a href=\"http://modernizr.com/\">modernizr</a>, etc.) for you. For example, let&#39;s put up 2 kittens side-by-side in jade:</p>\n<pre><code class=\"lang-jade\">.row\n  .col-sm-6\n    +img(&#39;img/kitten.jpg&#39;)\n  .col-sm-6\n    +img(&#39;img/kitten.jpg&#39;)\n</code></pre>\n<p>The end result:</p>\n<img src=\"img/first_proj.png\"/>"; }});